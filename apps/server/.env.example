# Local dev default matches infrastructure/docker-compose.yml; Railway sets
# these itself (PORT is injected automatically, REDIS_URL via a private
# networking variable reference to the Redis service).
PORT=8080
REDIS_URL=redis://localhost:6379
